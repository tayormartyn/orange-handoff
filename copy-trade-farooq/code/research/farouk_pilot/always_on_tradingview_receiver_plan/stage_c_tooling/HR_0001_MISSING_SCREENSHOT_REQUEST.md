# HR-0001 — Missing Screenshot Request

> **✅ RESOLVED (2026-07-09).** The two corrected screenshots below **were supplied and validated**
> (true 15m TF=15; Jul-9 1h TF=60). The 1h confirmed a multi-day downtrend into the anchor →
> **HTF BEARISH, opposes the LONG (counter-trend)**, so the provisional `SHADOW_CANDIDATE_MEDIUM`
> **reverted to `SHADOW_CANDIDATE_LOW`** and HR-0001 is now **REVIEWED / closed**. This request is
> retained for the record only — **no further screenshots are needed to close HR-0001.** See
> `HUMAN_REVIEW_HR_0001_RESULT.md` and `HUMAN_REVIEW_HR_0001_FORM.md`. Still observation-only; not
> trade-ready. The original request text is kept below for history.

---

The HR-0001 review was **held open at `NEEDS_MORE_DATA`** (now resolved — see banner above). The provisional
label `SHADOW_CANDIDATE_MEDIUM` was **observation-only — not trade-ready, not demo-ready, not permission to
trade.** To close the review, the following two corrected screenshots were requested (and have since been
supplied). `NOT_INTEGRATION_READY` unchanged.

## Why

- The file labelled **`HR-0001_15m.png` is actually a 1m chart** (indicator TF panel = 1). No true 15m
  context was captured.
- The **`HR-0001_1h.png` covers ~May 20 – Jun 18**, not Jul 9 — so higher-timeframe bias around the
  anchor could not be reviewed. (The machine 1h proxy was insufficient-data; the 15m proxy hinted
  bearish, unverified.)

## Please capture (XAUUSD · Pepperstone)

Anchor = **2026-07-09 04:12 UTC**. Note: your chart clock appears to be **UTC+1**, so 04:12 UTC = **05:12
on your chart**. Centre each capture on that candle.

1. **True 15m chart** around the anchor — timeframe selector on **15m** (not 1m). Window: roughly a few
   hours either side of the anchor. Save as `HR-0001_15m_TRUE.png`.
2. **1h chart covering Jul 9** around the anchor — timeframe **1h**, scrolled so **2026-07-09** is in
   view (roughly Jul 7 → Jul 10). Save as `HR-0001_1h_JUL9.png`.

### Each screenshot must show
- **Price scale visible** and **time axis / clock visible**.
- **State the chart timezone** (it looks like UTC+1 — please confirm the footer, e.g. "… UTC+1").
- **No account / balance / order tickets / positions / P&L / login / personal info** — crop them out.

### Save to
`stage_c_tooling/human_review_screenshots/HR-0001/`

## Optional (strengthens the record, not required to close)
- Telegram/Discord confirmation that the Farouk channel flagged this A-LONG (integrity check only — not a
  trade signal).

## After you provide them
Tell me and I'll finish the HR-0001 review: confirm/deny the HTF bias, finalise the label (MEDIUM holds,
or reverts toward LOW if HTF is bearish against the LONG), and set status to `REVIEWED`. Still
observation-only — no trade, no order, no broker.
