# Follower Constitution v0.1 — Readable Specification

**REVIEW-ONLY SHADOW PROPOSALS. NO BROKER ACTION. NO ORDER SUBMISSION.**
Machine-readable authority: `follower_constitution_v0_1.json` (this document explains; the JSON governs).
Status: DRAFT_PENDING_RATIFICATION → becomes RATIFIED when Martyn answers the four Q-items below.

## What this is

A deterministic rulebook that converts a frozen Farouk gold campaign capture into a reviewable
paper proposal and a fully reproducible theoretical follower result. Every policy is classed as
**FAROUK_PUBLISHED_RULE** (evidenced in his posts/corpus), **ORANGE_DESIGN_CHOICE** (we choose,
document, and expose sensitivity), or **UNKNOWN_FAIL_CLOSED** (nothing is invented; state pauses).

## Two permanent lanes

- **LANE A — STRICT_FOLLOWER:** the ratified defaults, applied exactly. Headline lane, always.
- **LANE B — POLICY_SENSITIVITY:** only the documented alternates (touch-fill, campaign-average BE,
  $3 graze tolerance per VR-21, keep-resting unfilled legs). **Never labeled as Farouk's actual
  result.** The more profitable lane is never promoted to headline.

## Conventions

1 pip = $0.10 (XAUUSD, project-established). Campaign unit = 1.0 split equally across proposed legs.
All arithmetic `decimal.Decimal`. Timestamps UTC from source evidence. Adjudication feed:
Pepperstone 1m (feed id recorded); grazes ≤ $3.00 logged AMBIGUOUS_FILL_BAND. Same-bar conflicts:
adverse-first ("stop-first-on-touch") + AMBIGUOUS_SEQUENCE tag.

## Policy summary (authority = JSON; ids P01–P24)

Campaign only from a Farouk direction+zone+SL post (P01); one campaign per entry post, fail-closed
grouping (P02); 3 equal legs near/mid/far [Q1] (P04); trade-through fills, Lane B touch (P05);
market-fill nearest leg at signal-bar close when price is already in-zone (P06); per-leg BE for
unscoped "sl to entry" [Q2], scoped instructions bind their named leg (P10); close-worst = worst
filled leg, single-leg case closes 50% (P11); "take 50% off" = 50% of open; instruction-driven TP1 =
50% of open; "take some" = 25% [Q4] (P12); runner rides until FINAL_CLOSE / SL / BE (P13); unfilled
legs cancel on SL_TO_ENTRY [Q3], Lane B keeps them resting (P14); re-entries followed to attempt 2,
attempt ≥3 skipped per R2 (P15); missed zone = NO_FILL, never chase (P16); posted SL binds all legs,
personal-stop claims are annotations (P21); contradictions pause the campaign (P20); prohibited
execution fields banned by guard (P24).

## Acknowledgement

REVIEW → ACKNOWLEDGED / REJECTED is Martyn's annotation. It changes a display state and nothing else.
