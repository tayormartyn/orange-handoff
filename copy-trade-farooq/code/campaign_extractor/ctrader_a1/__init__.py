"""
cTrader A1 — OFFLINE security + configuration preflight for a later controlled, view-only A2
connection. Connects to NOTHING, retries no OAuth, places no order. Reports only MASKED
presence states; never displays, hashes, or partially reveals any credential value.
"""
A1_VERSION = "ctrader-a1.0"
