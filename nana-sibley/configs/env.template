# ============================================================================
# Nana Sybil's Hearth — environment template (sanitized for handoff)
# No real secret was ever present in the repo (all code reads env vars);
# this template lists every variable the pipeline expects.
# ============================================================================

# Script generation (hearth_pipeline.py, n8n workflows)
ANTHROPIC_API_KEY=        # set in Windows Credential Manager / DPAPI

# Voice generation (hearth_voice.py / hearth_voice_worker.py — ElevenLabs)
ELEVEN_API_KEY=           # set in Windows Credential Manager / DPAPI
ELEVEN_VOICE_NAN=         # ElevenLabs voice id for Nan Sybil
ELEVEN_VOICE_MAEVE=       # ElevenLabs voice id for Maeve
ELEVEN_VOICE_JESSE=       # ElevenLabs voice id for Jesse

# Dream intake / episode store (setup_airtable.py, hearth_pipeline.py)
AIRTABLE_PAT=             # set in Windows Credential Manager / DPAPI
AIRTABLE_BASE_ID=         # base id (identifier, not a secret, but env-provided)

# Future website Dream Book backend (DREAM-BOOK-architecture.md)
LLM_API_KEY=              # set in Windows Credential Manager / DPAPI
AGENT_WEBHOOK_SECRET=     # set in Windows Credential Manager / DPAPI
